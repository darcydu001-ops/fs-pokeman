{
  "name": "fs-pokeman",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "art": "node scripts/fetch-portraits.mjs && node scripts/flatten-portraits.mjs"
  },
  "devDependencies": {
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
